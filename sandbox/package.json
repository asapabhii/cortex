{
  "name": "@cortex/sandbox",
  "version": "0.1.0",
  "description": "Developer tool for observing and replaying Cortex behavior",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cortex-sandbox": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "keywords": [
    "cortex",
    "sandbox",
    "debugging"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cortex": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  }
}

